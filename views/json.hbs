<section class="get">
    <h3>Final Json objects</h3>
    <a href="/info">Load data from db</a>
    <div class="container">
        {{# each items }}
            <article class="item">
                {{#each this}}
                    <div class="row"> {{@key}} : {{this}} </div>
                {{/each}}
            </article>
        {{/each}}
    </div>
</section>